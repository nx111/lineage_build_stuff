From 2653cea279ffecbfcd145953b653be04528498b1 Mon Sep 17 00:00:00 2001
From: Tom Marshall <tdm.code@gmail.com>
Date: Wed, 2 Jan 2019 09:09:50 -0800
Subject: [PATCH 3/3] minizip: More build fixes

Change-Id: I9ec42ea5693274dd56fb728c5de147a58defdbbb
---
 src/contrib/minizip/miniunz.c | 3 ---
 src/contrib/minizip/minizip.c | 2 +-
 2 files changed, 1 insertion(+), 4 deletions(-)

diff --git a/src/contrib/minizip/miniunz.c b/src/contrib/minizip/miniunz.c
index c37c79b..0d1264c 100644
--- a/src/contrib/minizip/miniunz.c
+++ b/src/contrib/minizip/miniunz.c
@@ -321,7 +321,6 @@ static int do_extract_currentfile(uf,popt_extract_without_path,popt_overwrite,pa
     uInt size_buf;
 
     unz_file_info64 file_info;
-    uLong ratio=0;
     err = unzGetCurrentFileInfo64(uf,&file_info,filename_inzip,sizeof(filename_inzip),NULL,0,NULL,0);
 
     if (err!=UNZ_OK)
@@ -478,7 +477,6 @@ static int do_extract(uf,opt_extract_without_path,opt_overwrite,password)
     uLong i;
     unz_global_info64 gi;
     int err;
-    FILE* fout=NULL;
 
     err = unzGetGlobalInfo64(uf,&gi);
     if (err!=UNZ_OK)
@@ -512,7 +510,6 @@ static int do_extract_onefile(uf,filename,opt_extract_without_path,opt_overwrite
     int opt_overwrite;
     const char* password;
 {
-    int err = UNZ_OK;
     if (unzLocateFile(uf,filename,CASESENSITIVITY)!=UNZ_OK)
     {
         printf("file %s not found in the zipfile\n",filename);
diff --git a/src/contrib/minizip/minizip.c b/src/contrib/minizip/minizip.c
index ab228c6..bca32e6 100644
--- a/src/contrib/minizip/minizip.c
+++ b/src/contrib/minizip/minizip.c
@@ -223,7 +223,7 @@ static int isLargeFile(const char* filename)
 
   if(pFile != NULL)
   {
-    int n = FSEEKO_FUNC(pFile, 0, SEEK_END);
+    FSEEKO_FUNC(pFile, 0, SEEK_END);
     pos = FTELLO_FUNC(pFile);
 
                 printf("File : %s is %lld bytes\n", filename, pos);
-- 
2.17.1

