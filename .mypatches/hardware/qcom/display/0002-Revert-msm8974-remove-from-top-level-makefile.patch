From 7162dbcea494d7e67f7b9fd9c02cf5b4fd3ca4e9 Mon Sep 17 00:00:00 2001
From: Chirayu Desai <chirayudesai1@gmail.com>
Date: Wed, 24 Aug 2016 01:53:10 +0530
Subject: [PATCH 2/4] Revert "msm8974: remove from top level makefile"

This reverts commit 798662177c2d334ce59b232effd3315ee872a33a.

Change-Id: I63514641d6be40bc7d5d96dc3ce44ecfe9567c6a
---
 Android.mk | 29 +++++++++++++++++------------
 1 file changed, 17 insertions(+), 12 deletions(-)

diff --git a/Android.mk b/Android.mk
index 2f7f32e..9843b36 100644
--- a/Android.mk
+++ b/Android.mk
@@ -6,23 +6,28 @@ ifneq ($(TARGET_BOARD_AUTO),true)
     #This is for 8084 based platforms
     include $(call all-named-subdir-makefiles,msm8084)
   else
-    ifneq ($(filter msm8226 msm8x26,$(TARGET_BOARD_PLATFORM)),)
-      include $(call all-named-subdir-makefiles,msm8226)
+    ifneq ($(filter msm8974 msm8x74,$(TARGET_BOARD_PLATFORM)),)
+      #This is for 8974 based (and B-family) platforms
+      include $(call all-named-subdir-makefiles,msm8974)
     else
-      ifneq ($(filter msm8960,$(TARGET_BOARD_PLATFORM)),)
-        include $(call all-named-subdir-makefiles,msm8960)
+      ifneq ($(filter msm8226 msm8x26,$(TARGET_BOARD_PLATFORM)),)
+        include $(call all-named-subdir-makefiles,msm8226)
       else
-        ifneq ($(filter msm8994 msm8992,$(TARGET_BOARD_PLATFORM)),)
-          include $(call all-named-subdir-makefiles,msm8994)
+        ifneq ($(filter msm8960,$(TARGET_BOARD_PLATFORM)),)
+          include $(call all-named-subdir-makefiles,msm8960)
         else
-          ifneq ($(filter msm8909,$(TARGET_BOARD_PLATFORM)),)
-            include $(call all-named-subdir-makefiles,msm8909)
+          ifneq ($(filter msm8994 msm8992,$(TARGET_BOARD_PLATFORM)),)
+            include $(call all-named-subdir-makefiles,msm8994)
           else
-            ifneq ($(filter msm8996,$(TARGET_BOARD_PLATFORM)),)
-              include $(call all-named-subdir-makefiles,msm8996)
+            ifneq ($(filter msm8909,$(TARGET_BOARD_PLATFORM)),)
+              include $(call all-named-subdir-makefiles,msm8909)
             else
-              ifneq ($(filter msm8998,$(TARGET_BOARD_PLATFORM)),)
-                include $(call all-named-subdir-makefiles,msm8998)
+              ifneq ($(filter msm8996,$(TARGET_BOARD_PLATFORM)),)
+                include $(call all-named-subdir-makefiles,msm8996)
+              else
+                ifneq ($(filter msm8998,$(TARGET_BOARD_PLATFORM)),)
+                  include $(call all-named-subdir-makefiles,msm8998)
+                endif
               endif
             endif
           endif
-- 
2.7.4

